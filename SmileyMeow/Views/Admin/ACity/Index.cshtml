@model List<City>
@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="card">

    <div class="card-body">
        <a asp-controller="ACity" asp-action="Create" class="btn btn-primary crt-btn-m5">
            Create
        </a>

        <h5 class="card-title">Cities</h5>
        <div class="table-responsive">
            <table id="zero_config" class="table table-striped table-bordered">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Name</th>
                        <th class="last-clmn">Options</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (City city in Model)
                    {

                        <tr>
                            <td>@city.CityId</td>
                            <td>@city.CName</td>
                            <td><a asp-controller="ACity" asp-action="Delete"
                                asp-route-id="@city.CityId" class="icn-mr">
                                    <i class="fas fa-trash"></i>
                                </a>
                                <a asp-controller="ACity" asp-action="Update"
                                asp-route-id="@city.CityId" class="icn-mr">
                                    <i class="fas fa-pencil-alt"></i>
                                </a>
                                <a asp-controller="ACity" asp-action="Info"
                                asp-route-id="@city.CityId">
                                    <i class="fas fa-info"></i>
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

    </div>
</div>
</div>