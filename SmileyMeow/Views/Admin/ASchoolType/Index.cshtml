@model List<SchoolType>
@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="card">

    <div class="card-body">
        <a asp-controller="ASchoolType" asp-action="Create" class="btn btn-primary crt-btn-m5">
            Create
        </a>

        <h5 class="card-title">School Types</h5>
        <div class="table-responsive">
            <table id="zero_config" class="table table-striped table-bordered">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Name</th>
                        <th class="last-clmn">Options</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (SchoolType schoolType in Model)
                    {

                        <tr>
                            <td>@schoolType.SchoolTypeId</td>
                            <td>@schoolType.STName</td>
                            <td><a asp-controller="ASchoolType" asp-action="Delete"
                                asp-route-id="@schoolType.SchoolTypeId" class="icn-mr">
                                    <i class="fas fa-trash"></i>
                                </a>
                                <a asp-controller="ASchoolType" asp-action="Update"
                                asp-route-id="@schoolType.SchoolTypeId" class="icn-mr">
                                    <i class="fas fa-pencil-alt"></i>
                                </a>
                                <a asp-controller="ASchoolType" asp-action="Info"
                                asp-route-id="@schoolType.SchoolTypeId">
                                    <i class="fas fa-info"></i>
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

    </div>
</div>
</div>