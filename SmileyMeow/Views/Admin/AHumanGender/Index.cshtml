@model List<HumanGender>
@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="card">

    <div class="card-body">
        <a asp-controller="AHumanGender" asp-action="Create" class="btn btn-primary crt-btn-m5">
            Create
        </a>

        <h5 class="card-title">Genders</h5>
        <div class="table-responsive">
            <table id="zero_config" class="table table-striped table-bordered">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Name</th>
                        <th class="last-clmn">Options</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (HumanGender humanGender in Model)
                    {

                        <tr>
                            <td>@humanGender.HumanGenderId</td>
                            <td>@humanGender.GName</td>
                            <td><a asp-controller="AHumanGender" asp-action="Delete"
                                asp-route-id="@humanGender.HumanGenderId" class="icn-mr">
                                    <i class="fas fa-trash"></i>
                                </a>
                                <a asp-controller="AHumanGender" asp-action="Update"
                                asp-route-id="@humanGender.HumanGenderId" class="icn-mr">
                                    <i class="fas fa-pencil-alt"></i>
                                </a>
                                <a asp-controller="AHumanGender" asp-action="Info"
                                asp-route-id="@humanGender.HumanGenderId">
                                    <i class="fas fa-info"></i>
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

    </div>
</div>
</div>