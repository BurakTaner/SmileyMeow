@model List<Pronoun>
@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="card">

    <div class="card-body">
        <a asp-controller="APronoun" asp-action="Create" class="btn btn-primary crt-btn-m5">
            Create
        </a>

        <h5 class="card-title">Pronouns</h5>
        <div class="table-responsive">
            <table id="zero_config" class="table table-striped table-bordered">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Name</th>
                        <th class="last-clmn">Options</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (Pronoun pronoun in Model)
                    {

                        <tr>
                            <td>@pronoun.ProunounId</td>
                            <td>@pronoun.PName</td>
                            <td><a asp-controller="APronoun" asp-action="Delete"
                                asp-route-id="@pronoun.ProunounId" class="icn-mr">
                                    <i class="fas fa-trash"></i>
                                </a>
                                <a asp-controller="APronoun" asp-action="Update"
                                asp-route-id="@pronoun.ProunounId" class="icn-mr">
                                    <i class="fas fa-pencil-alt"></i>
                                </a>
                                <a asp-controller="APronoun" asp-action="Info"
                                asp-route-id="@pronoun.ProunounId">
                                    <i class="fas fa-info"></i>
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

    </div>
</div>
</div>